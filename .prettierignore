.svelte-kit/**
static/**
build/**
node_modules/**
.netlify/**
.github/ISSUE_TEMPLATE/**
.claude/**
# Generated by `npm run tokens` — prettier would reformat it and the next
# generate would revert that, so the two would trade the file back and forth.
src/hyzer-tokens.css
